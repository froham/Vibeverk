> **Archived 2026-07-19.** This is the pre-2026-07-19 body of `docs/roadmap/ROADMAP.md`, moved here verbatim (not edited, not deleted). That file had grown into a second changelog -- long, multi-paragraph "Status YYYY-MM-DD" updates nested inside "Next" items going back to 2026-07-07 -- rather than showing actual current Current focus / Next / Later. This archive is not authoritative and was not re-verified against current reality at the time of archiving. Treat `docs/roadmap/ROADMAP.md`, `docs/project/CURRENT_STATE.md`, and `docs/project/CHANGELOG.md` as authoritative over anything below -- this file is kept only for the detailed historical reasoning behind decisions and completed work. See `docs/archive/README.md` for the archiving convention.

---
# Vibeverk — Roadmap

Roadmap content is planning material. It is not proof of current functionality, architecture, security posture or customer commitments. For verified current state, see [`docs/project/CURRENT_STATE.md`](../project/CURRENT_STATE.md). For historical detail behind completed and paused work, see [`docs/archive/roadmap-2026-07-01.md`](../archive/roadmap-2026-07-01.md).

Sist oppdatert: 2026-07-15.

## Overordna mål

**Retning stadfesta 2026-07-07** (Arkitekt-godkjent, sjå `docs/decisions/ADR-0008-control-plane-data-plane-split.md`): Vibeverk skal skalere frå éin kunde til 10-100 SMB-kundar via éin sentral Console (`console.vibeverk.no`, control plane) og eitt Supabase-prosjekt per kunde (data plane, uendra prinsipp) — IKKJE via repo-fork-per-kunde.

**Den tekniske sida av denne planen (Fase 1–9) er no fullført, pusha og deployert** — sjå "Fullført" under. Dette supersederer den eldre "eksempel-/demokunde via eigen repo-fork"-planen (`hub/tenants.js`-basert), som var eit mellombels stillas, ikkje målarkitekturen.

**Retningsavklaring 2026-07-10 (brukar)**: no som den tekniske blokkeringa er borte, flyttar fokus seg frå å *byggje* tenant-systemet til å faktisk *bruke* det — ein reell showcase-kunde, Vibeverk sjølv som ekte tenant, og sterkare personvernarbeid. Sjå "Current focus" og "Next" under.

## Fullført

**SaaS-skaleringsplanen, Fase 1–9 (2026-07-07 til 2026-07-10), alle pusha til `main`:**

1. **Fase 1 — Tryggleiksgjeld.** Full sikkerheitsaudit + personvernvurdering av éin-kunde-koden, alle HIGH/BLOCKER-funn lukka og stadfesta i produksjon. Fullført 2026-07-07.
2. **Fase 2 — Hostingarkitektur.** Vercel valt over Cloudflare/Netlify (apex-domene utan nameserver-migrering var avgjerande). Mekanismebevis (Vercel Routing Middleware) stadfesta.
3. **Fase 3 — Ekte SQL-migrasjonar.** `supabase/migrations/` er den CLI-deployerbare kjelda (baseline `20260707000001`); `migration.sql` er frose.
4. **Fase 4 — Asynkron config-bootstrap.** `App.ready(fn)`-gate i alle modular, plumbing for at config kan hentast dynamisk (føresetnad for Fase 6).
5. **Fase 5 — Namnebyte intranet → Workspace (Tier 1).** Katalog, filnamn, testnamn, URL-stiar oppdaterte. Tier 2 (`window.Intranet`, `intranettFeatures`-nøkkelen) bevisst utsett, sjå `CLAUDE.md`.
6. **Fase 6 — Reell hostname→tenant-oppløysing.** Den siste faktiske blokkeringa for kunde nr. 2. Bygd, Security Auditor-gjennomgått (2 rundar), deployert til Vercel, og **stadfesta live ende-til-ende 2026-07-09** med ein kanarikunde (`phase6-canary`): registrering → tilkopling → hemmeleg-lagring → skjema/RLS-verifisering → reell DNS/HTTP-rutingverifisering → aktivering, alt via ekte kode. Ein akseptert-for-no eksponeringsperiode (H2, provisioning-tenantar publikt oppløyselege før RLS-verifisering) vart **lukka heilt 2026-07-09**.
7. **Fase 7 — Control plane.** Eige Supabase-prosjekt `vibeverk-control` (tenant-register, operatørar, RLS, `resolve_tenant_by_hostname()`, Vault-basert hemmeleg-lagring). Sjå `docs/decisions/ADR-0008`.
8. **Fase 8 — Console mot control plane.** Console autentiserer no mot `vibeverk-control` (OTP), ikkje mot kunden sitt eige prosjekt. Alle kundehandlingar går via ein audit-logga `broker`. Sjå `docs/decisions/ADR-0009`.
9. **Fase 9 — Semi-automatisert onboarding v1.** Ny "Kundar"-seksjon i Console (liste, registreringsskjema, per-kunde sjekkliste), `tenant-admin` Edge Function. Hard-gata frå aktivering til Fase 6 fanst. Sjå `docs/decisions/ADR-0010`.

**Etter dette, 2026-07-09/10: fem-kjelde parallell tryggingsgjennomgang** — ein ekstern Codex-tryggingsrunde (køyrd av brukar sjølv) pluss fire parallelle, skrivebeskytta Claude-agentar (privacy-compliance, arkitekt, QA, ux-mobile). Synteserte til éin rapport; dei fire høgast-prioriterte funna (Console write-race, CFG-fallback-lekkasje i Console, redirect-hol og IPv6-hol i SSRF-sjekken) er fiksa, testa og deployerte (`v0.27.2`). Full detalj: `docs/project/CHANGELOG.md` og `docs/decisions/ADR-0007`.

**Konklusjon: det finst i dag ingen teknisk blokkering for å onboarde ein ekte kunde nr. 2.** Attverande punkt er akseptert-for-no restrisikoar (DNS-rebinding, at `schema_verified_at` ikkje vert revalidert før aktivering) — dokumenterte i ADR-0007, ikkje kritiske.

## Current focus

Fokus har flytta seg frå å byggje tenant-systemet til å faktisk *bruke* det. **Stadfesta 2026-07-13 (brukar): før noko av dette, skal punkt 0 under (forklaringstekster/brukervennlegheit) på plass** — sjå "Next".

## Avgjerder teke 2026-07-13

Tre opne spørsmål frå backup/restore-gjennomgangen same dag vart avklara direkte av brukar (sjå `docs/project/CURRENT_STATE.md` for full funn-kontekst):

- **Oppgåver (`tasks`) — RLS skal strammast inn, IKKJE berre UI.** `module-tasks.js` viser alt berre "Mine oppgåver"/"Tildelt til andre av meg" til ein member (kun admin/eigar ser "Andre sine oppgåver") — men RLS (`tasks_read`-policyen) tillèt i dag `SELECT` på ALLE rader for enhver autentisert rolle, uavhengig av UI. Avgjort: databasen skal stemme overeins med det UI-et alt gjev seg ut for å vise — ein member skal berre kunne `SELECT` rader der `assigned_to = seg sjølv ELLER created_by = seg sjølv`. **Fullført og live i produksjon 2026-07-13** (`supabase/migrations/20260713130659_tighten_tasks_read_rls.sql`, PR #26) — verifisert mot `vibeverk-staging` FØR produksjon, deretter stadfesta direkte mot produksjonen sin `pg_policies`. Kunngjeringar/kunnskapsbase/lenker sin tilsvarande "alle les alt"-policy er stadfesta **bevisst design** (oppslagstavle-innhald for heile bedrifta) og rørast ikkje. CRM/leads/bookingar sin brei lesetilgang for member er også stadfesta som truleg tiltenkt (matchar den dokumenterte rollemodellen "member: les alt") — ikkje endra, men heller ikkje eksplisitt dobbeltsjekka mot brukar utover dette.
- **Chat skal IKKJE inkluderast i "full" sikkerhetskopi.** Avgjort: chat-innhald er meir flyktig support-korrespondanse enn forretningsdokumentasjon (i motsetnad til CRM/leads/bookingar), med større volum og ein reell personvernkostnad ved å liggje ukryptert i ei nedlastbar fil. Handterast likt som `notes` — ei bevisst, dokumentert utelating (alt delvis reflektert i UI-teksten frå same dag), ikkje eit hol som manglar å tettast.
- **Showcase-kunde: sjå punkt 1 under Next.**

## Next

0. **Forklaringstekster og brukarvennlegheit i heile løysinga — MÅ vere på plass før neste kunde (Sunnvask) vert testa.** Stadfesta 2026-07-13 (brukar): talet på funksjonar veks no raskt på tvers av superadmin (Console), kundeadmin og editor/member-rollane, og det er kritisk at løysinga held fram med å vere SUPERENKEL og forståeleg — dette ER sjølve forretningsideen (å hjelpe SMB-kundar ta i bruk digitale verktøy dei faktisk forstår, ikkje berre eit teknisk produkt). Alle stader i løysinga treng gode, ikkje-tekniske forklaringar, placeholders og tooltips — språket skal FORKLARE, aldri bruke fagterminologi. Særleg i Console (superadmin sitt verktøy for kundeoppsett/-endring) skal det finnast ei komplett, idiotsikker sjekkliste — det skal ALLTID vere utvilsamt kva som skjer når nokon lagrar eller endrar noko, same kvar i løysinga.

   **Arkitekt-konsultasjon gjennomført 2026-07-13** (les-berre, ingen kode endra). Hovudfunn: dei rette byggeklossane finst ALT (`helpIcon()` og `field({hint})` i `components.js`, ein delt `bindHelpIcons()`), problemet er manglande DISIPLIN i bruken, ikkje mangel på mønster. Fann og eitt reelt, ubunde funn undervegs: `workspace/index.html` sin CSS definerer berre `.form__status.is-err` (ikkje `is-error`), medan koden i `module-announcements.js`/`module-settings.js` faktisk set klassen `is-error` — feilmeldingar i Workspace vert difor usynlege/ustila i dag, ei konkret smakebit på kva spreidde tekstforbetringar utan felles kjelde fører til.

   **Fase 2 gjennomført 2026-07-13 (v0.33.2) — Workspace sine destruktive handlingar**: retta ein reell unøyaktigheit i `module-settings.js` sin "Nullstill intranett-data"-dialog (hevda "All intranett-data slettes permanent", medan funksjonen faktisk berre tømmer fire spesifikke nøklar — no i samsvar med den alt-korrekte hjelpeteksten over knappen). `workspace/module-users.js` sin fjern-brukar-dialog forklarer no at forfatta oppgåver/kunngjeringar/KB-artiklar IKKJE vert sletta. Den funne CSS-drifta (`is-err`/`is-error`) er retta. Ikkje rørt: dei enklare enkelt-element-slett-dialogane (vurdert som gode nok som dei er).

   **Fase 3 gjennomført 2026-07-13 (v0.33.3) — Web-admin sine paneler**: `module-crm.js` sin "Slett kunde"-dialog (to identiske stader) vart FØRST verifisert mot faktisk implementasjon (`deleteAllForEmail()`+`deleteCustomer()` dekker faktisk leads/tilbod/bookingar/kommunikasjon/chat) — omfanget var alt korrekt, men mangla eksplisitt "kan ikkje angrast" og presisering av kva som fjernast, no retta. `module-users.js` (Web-admin sin eigen, root-nivå fil) fekk same forfattar-innhald-presisering som Workspace-versjonen i fase 2. GDPR-slettingsskjemaet sin dialog vart sjekka mot `deleteByEmail()` og funne allereie korrekt — ikkje endra.

   **Fase 4 starta 2026-07-13 (v0.33.4), slice 1 — Console sine 25 funksjonsbrytarar**: `FEAT_LABELS`/`IFEAT_LABELS` i "Modular"-fana (t.d. "Native e-post" for `crmFull`) hadde null forklaring før. `checkboxGrid()` fekk eit valfritt `help`-parameter, nye `FEAT_HELP`/`IFEAT_HELP`-map gjev éin-setning-forklaring per brytar via `helpIcon()`. **Reelt funn undervegs**: `console/index.html` mangla HEILT `.help-icon`/`.help-icon__pop`-CSS (dei tre andre flatene hadde det alt) — utan denne ville tooltip-teksten vist seg alltid synleg i staden for skjult til klikk. Retta, stadfesta empirisk via jsdom (popover `display:none` FØR klikk, `display:block` ETTER). **Dette er berre første bit av eit ope-slutta steg** — Console sine andre fanar (Web/Workspace-tema, Analyse, Personvern) og dei to andre flatene sine ikkje-destruktive felt er ikkje gjennomgått enno. Held fram gradvis, sjå `docs/architecture/copy-style-guide.md`.

   **Fase 1 gjennomført same dag (v0.33.0)** — sjå `docs/project/CHANGELOG.md` 0.33.0 for full detalj: ny `docs/architecture/copy-style-guide.md`, `field()` utvida med `help`-parameter, og dei tre høgast-konsekvens-hola i Console retta ("Set aktiv" hadde FØR ingen forklaring i det heile trass i at han gjer ein kunde live; "Arkiver kunde"/"Nullstill all konfig" oppgraderte til full Nivå B). IKKJE gjort enno: den generaliserte `.kd-card`-hjelpefunksjonen (tilrådd, men medvite utsett som eige, verifisert steg — Console har ingen automatisk testdekning), Workspace, Web-admin, og den generelle tooltip-runden. UX/Mobile Reviewer-gjennomgang av denne runda er tilrådd, ikkje enno køyrd.

   **Tilrådd konkret plan** (resten, sjå rekkjefølgja under):
   - IKKJE bygg ein ny komponent — utvid `field()` med eit valfritt `help`-felt (attåt det alt eksisterande `hint`) som rendrar `helpIcon()` ved sida av label, i staden for at kvar modul handrullar begge.
   - To tydelege nivå for lagre-/slette-kommunikasjon: **Nivå A** (reversibelt — inline hint/hjelpetekst før, `form__status`-tilbakemelding etter, ingen `confirm()`) og **Nivå B** (destruktivt/irreversibelt — `confirm()` som ALLTID seier kva som vert påverka, kva som IKKJE vert påverka, og om det kan angrast; `core.js` sin backup-import-dialog frå i dag er referanseeksempelet). Ein tredje "Nivå B-inline" for Console (åtvaring ved sida av lagre-knapp for høg-konsekvens vanlege lagringar, t.d. domeneendring på ein aktiv kunde).
   - Generaliser Console sin `.kd-card`-sjekkliste-mal (frå Fase 9-onboardinga) til EIN delt intern hjelpefunksjon i `console-core.js`, brukt for alle konsekvenstunge Console-handlingar (arkivere, domeneendring, hemmeleg-rotering), ikkje berre onboarding.
   - Skriv retningslinjene ned i ein ny `docs/architecture/copy-style-guide.md` (kryssreferert frå `docs/README.md` og `CLAUDE.md`), pluss ein kort peikar-kommentar i `components.js` ved `helpIcon()`/`field()`.
   - Rekkjefølgje: **Console sine destruktive handlingar først** (minst filflate, høgst konsekvens per feil, skriv stilguiden samstundes) → **Workspace sine destruktive handlingar** (fiks òg `is-err`/`is-error`-drifta same runde) → **Web-admin** → **generelle tooltips/hint overalt** til slutt, opportunistisk modul for modul.
   
   **Status 2026-07-15 — planen sin tilrådde rekkjefølgje (Console → Workspace → Web-admin → generelle tooltips overalt) er no gjennomført éin runde, ikkje avslutta som eit ferdig prosjekt.** Alle tre flatene sine DESTRUKTIVE handlingar er dekte (fase 1–3, sjå over). Den opne-slutta "generelle tooltips"-delen (fase 4) har hatt éin survey-og-fiks-runde per flate, basert på ein Explore-agent sin kartleggingsrapport, IKKJE ein linje-for-linje-audit av kvart einaste felt:
   - **Console**: "Modular"-fana sine 25 funksjonsbrytarar (0.33.4) pluss "Web"- og "Analyse"-fanene sine sjargong-tunge felt (0.34.8) er retta. "Personvern"-fana og resten av Web-admin/Workspace-fanene vart sjekka og funne allereie tilstrekkeleg forklarte.
   - **Workspace**: `module-users.js` (rolle-hint), `module-kb.js` og `module-notes.js` (fjerna ein reelt feilaktig "AI-kontekst/AI-sammendrag"-påstand — ingen AI-funksjon finst i kodebasen), `module-orgdrift.js` (fire uklare felt) er retta (0.36.2). `module-settings.js` sitt "Bedriftsnavn" (skjult Console-overstyring) og "Kontakt-e-post" (lagra, men aldri lese nokon annan stad i kodebasen) er retta (0.36.3). Resten av dei kartlagde Workspace-modulane (workspaceship, tasks, contact, booking, links, quote, mediabank-internal, dashboard, announcements) er sjekka og funne allereie tilstrekkeleg forklarte.
   - **Web-admin**: `module-chat.js` sitt admin-bakgrunnsfargefelt og `core.js` sitt footer-copyright-felt er retta (0.36.1); resten av Web-admin (booking/tilbod/referansar/faq/mediebank/crm) vart sjekka og funne allereie dekt.
   - **Ikkje uttømmande** — fleire modular/felt kan framleis ha hol; dette var ei kartlagt, prioritert utbetring, ikkje ein full gjennomgang av heile løysinga. Held fram opportunistisk, modul for modul, som opphavleg planlagt.

   **Lukka som eige punkt av brukar 2026-07-17**: kontinuerleg testing + tilbakemelding (sjå heile 2026-07-17-økta: knapp-radius, referanse-tekst-kutting, Læring-omsetjing m.m. vart alle fanga opp og retta same dag denne vegen) erstattar no dette som ei formell, "å bli ferdig med"-oppgåve. Nye konkrete hol handterast som dei kjem, ikkje som eit ope punkt på lista.
1. **Eksempelkunde ("Sunnvask") som reell showcase, bygd frå eit heilt nytt oppsett.** Avgjort 2026-07-13: den gamle `phase6-canary`/`vibeverk-j1yg`-testraden **slettast heilt**. Ny demo-/eksempelkunde med namnet "Sunnvask" byggjast frå botnen av via Console sin ekte, no-ferdige onboardingsflyt — nettopp for å stadfeste at ein heilt fersk kunde faktisk kjem korrekt gjennom heile flyten denne gongen (canary-runden 2026-07-09 var eit mekanismebevis for sjølve rutingen, ikkje ein fullstendig kvalitetssikra gjennomgang av heile onboardinga). **Tilrådd av Claude same dag, ikkje enno endeleg stadfesta av brukar i detalj**: gje Sunnvask sitt EIGE, dedikerte Supabase-dataplan-prosjekt — IKKJE gjenbruk `vibeverk-staging` som dataplan. Grunngjeving: staging må halde fram som eit reint, fritt disponibelt testområde for framtidige migrasjonar (kan trengje å tømmast/øydeleggjast midlertidig under ein migrasjonstest, slik det vart brukt heile 2026-07-13-runden), medan ein demo-kunde til vising alltid må sjå presentabel og stabil ut — å blande dei to formåla ville anten gjere framtidig migrasjonstesting risikabel for demoen, eller krevje oppussing av demoen kvar gong. To separate, billige prosjekt er ein liten kostnad mot den risikoen.

   **Status 2026-07-14 — gjennomført som reell, fyrste ende-til-ende-validering av fersk-prosjekt-onboarding, IKKJE enno aktivert.** Sunnvask er, per denne økta si eiga skildring, no registrert med sitt EIGE, dedikerte Supabase-prosjekt (rapportert ref `nzgibflxodcwuhtaprrs`) — ikkje delt med `vibeverk-staging`, nøyaktig som avgjort over. Den gamle `sunnvask`-tenant-rada (som brukte delt `vibeverk-staging` som dataplan, eit mellombels stillas frå før denne avgjerda) er omdøypt til `staging` via Console sitt eige slug-redigeringsfelt, for å frigjere `sunnvask`-namnet/identiteten. Den tidlegare `Test-vercel`-testrada (brukt til å verifisere sjølve Vercel-rutingmekanikken) er arkivert. Dette er den fyrste gongen fersk-prosjekt-onboardingsvegen faktisk er køyrd frå botnen av — all tidlegare testing denne økta (og tidlegare) attbrukte alt-eksisterande prosjekt. Fleire reelle, tidlegare-latente feil vart funne og retta undervegs (sjå `docs/project/CHANGELOG.md` 0.34.1–0.34.7 og `docs/project/CURRENT_STATE.md` sitt 2026-07-14-oppslag for full detalj): automatisert SMTP-oppsett (`configure_tenant_smtp`), ein reell rolletidspunkt-feil i `handle_new_user()` som gjorde at inviterte admin-brukarar hamna som `member`, ein påfølgjande same-dag-regresjon i sjølvopprykk-vernet, og ein hash-timing-feil i `workspace-core.js` sin `boot()` som hoppa over "Vel ditt passord"-steget. Denne tenant-registerstatusen (prosjektreferanse, slug-omdøyping, arkivering) er live databasetilstand i `vibeverk-control`, ikkje synleg noko stad i dette repoet — sjå `docs/project/CURRENT_STATE.md` "External verification required" for det skiljet.

   **Lukka som eige punkt av brukar 2026-07-17**: den ferdige presentable oppsettet (steg 8) og sjølve aktiveringa av Sunnvask er ikkje lenger tracka som eit ope roadmap-punkt — handterast som ei vanleg, seinare driftsoppgåve når/om det faktisk trengst, ikkje noko som blokkerer anna arbeid.
2. **Vibeverk sjølv som reell tenant i systemet — FULLFØRT 2026-07-16, sjå status under.** Fram til 2026-07-16 køyrde Vibeverk sin eigen produksjon (`vibeverk.no`) UTANFOR tenant-modellen — statisk `config.js`, direkte mot sitt eige Supabase-prosjekt (`clzczbyklgdtdhgjphup`), ikkje registrert i `vibeverk-control` sitt tenant-register. Brukar ønskte at Vibeverk sjølv («den reelle aktøren») også skulle nytte plattforma gjennom same tenant-system som andre kundar, ikkje som eit særtilfelle — no gjennomført: `vibeverk.no` sin DNS peikar på Vercel, og sida vert servert via `middleware.js`/`api/tenant-config.js` sin hostname-oppløysing mot den eksisterande, stadfesta korrekte `vibeverk.no`-tenant-rada i `vibeverk-control`, akkurat som for andre kundar. Dette var ei **arkitektur-sensitiv endring som rørte reell produksjonstrafikk** — gjekk via Arkitekt-agenten FØR gjennomføring (jf. CLAUDE.md sin regel om arkitekturendringar), med eksplisitt godkjenning per steg som vanleg. Hang saman med punkt 1 (same mekanisme, ulikt formål).

   **Status 2026-07-13 — mekanisme stadfesta, DNS-cutover ikkje gjort:**
   - Fann eit allereie-eksisterande, historisk `vibeverk.no`-tenant-rad i `vibeverk-control` med `status='active'` men aldri verifisert (frå tidlegare fase-mekanismebevis, sett aktiv utanom den vanlege sjekklistefloyten).
   - Fann og eit separat, allereie-eksisterande Vercel-prosjekt kalla "vibeverk" (`vibeverk.vercel.app`, skilt frå den lokale repoen sin lenka `vibeverk-j1yg`-kanarie) — auto-deployer på kvar push til `main`, hadde `middleware.js` aktiv men INGEN miljøvariablar sett (difor 500 på `/config.js`).
   - Sette `VIBEVERK_CONTROL_URL`/`VIBEVERK_CONTROL_ANON_KEY` på dette Vercel-prosjektet (prod+preview) og redeploya — stadfesta at ruting-mekanismen (middleware + `api/tenant-config.js`) no fungerer korrekt: avviser trygt eit ikkje-registrert hostname (`vibeverk.vercel.app` sjølv) med ein rett 404 i staden for det tidlegare, øydelagde 500-svaret.
   - La til `vibeverk.no` som eit (uverifisert, ingen ekte trafikk enno — DNS peikar framleis på GitHub Pages) domene-alias på Vercel-prosjektet, som eit reint førebuande steg for ein eventuell seinare DNS-cutover.
   - **Skjema verifisert ✓** for den eksisterande `vibeverk.no`-tenant-rada — fann og fiksa ein reell produksjonsfeil undervegs: `verify_schema_fingerprint()` mangla eksplisitt `GRANT EXECUTE ... TO service_role` (begge migrasjonane som oppretta funksjonen `REVOKE`-a berre frå `PUBLIC`/`anon`/`authenticated`, gløymde `service_role`) — retta med ny migrasjon, stadfesta via `has_function_privilege`.
   - **Ruting verifisert ✓** for same rad — `verify_tenant_routing` var før hard-gata til berre `status='provisioning'`, som gjorde det umogleg å verifisere ein allereie-`active` rad utan å reversere han (farleg, sidan `resolve_tenant_by_hostname()` handsamar dei to statusane ulikt). Arkitekt-designa utviding tillèt no `provisioning` ELLER `active`; sikra av ein sikkerheitsgjennomgang (sjå under) og deploya til `vibeverk-control`.
   - **Nytt bruk av ein alt kjend, standande grense**: `.claude/agents/vibeverk-security-auditor.md`/`vibeverk-reviewer.md` finst ikkje i Claude-harnesset (berre `.codex/agents/*.toml`-variantar gjer det) — dette var alt dokumentert i `docs/project/CURRENT_STATE.md` frå 2026-07-04/2026-07-13, ikkje ei ny oppdaging denne runda. Sikkerheitsgjennomgangen av `verify_tenant_routing`-endringa vart difor gjort av ein general-purpose-agent med eit tilsvarande sikkerheitsprompt, same arbeidsrunde som løyste dette praktisk før. **Uavklart, sjå CURRENT_STATE.md**: skal `vibeverk-security-auditor.md`/`vibeverk-reviewer.md` faktisk portast til Claude-harnesset, eller skal CLAUDE.md/`vibeverk-handoff`-skillen omformulerast slik at dei ikkje les som om alle sju agentane er symmetriske?
   - **Ikkje gjort, medvite utsett**: sjølve DNS-cutoveren (A-post eller nameserver-byte som faktisk flyttar ekte trafikk frå GitHub Pages til Vercel) — dette er framleis ei eiga, seinare, eksplisitt godkjend avgjerd. Same for den brukar-føreslegne `console.vibeverk.no`-fråkoplinga (eige Vercel-prosjekt/subdomene for Console, for å fjerne kopling til delt tenant-oppløysing) — føreslått og vurdert som fornuftig, men ikkje bygd.
   - **Status 2026-07-14 — ingen ny direkte framdrift på sjølve `vibeverk.no`-tenant-rada eller DNS-cutoveren, men indirekte styrka**: 0.34.0 sitt "tenant admin-user bootstrap"-arbeid (`invite_tenant_admin`/`generate_support_access`) vart funne naudsynt "while dry-run testing the vibeverk-as-tenant migration" (sjå `docs/project/CHANGELOG.md` 0.34.0), og fleire onboardingsfeil funne same runde (invitert-rolle-tidspunkt, hash-timing i Workspace-login, SMTP-oppsett) gjer heile onboardingsvegen — inkludert den `vibeverk.no` sjølv til slutt skal gjennom — meir robust. DNS-cutoveren og statusen elles var **uendra** frå 2026-07-13-oppslaget over.
   - **Status 2026-07-16 — forarbeidet FULLFØRT og stadfesta OK, DNS-cutoveren sjølv framleis ikkje gjort.** Brukar godkjende å gå vidare med dette punktet; Arkitekt-agenten vart konsultert på nytt (les-berre) og kravde tre ting stadfesta FØR nokon DNS-endring vart føreslått konkret:
     1. **Tenant-raden sitt faktiske innhald** — verifisert direkte (Dashboard SQL, feltvis samanlikna mot repoet sin `config.js`): `data_plane_url`, `data_plane_anon_key` (byte-for-byte identisk), `data_plane_storage_key`, `product_mode`, `enabled_modules.features`/`.intranettFeatures` (15+10 flagg), `theme` — **alt alt korrekt**, ingen retting naudsynt. Arkitekten sin føre-hands-bekymring (basert på at raden vart sett aktiv utanom sjekklista) synte seg ikkje å stemme i praksis.
     2. **Accept-path smoke-test** (aldri gjort før — berre reject-path/404 var stadfesta tidlegare): `vibeverk.vercel.app` vart mellombels registrert som eit ekstra hostname-alias på den ekte `vibeverk`-tenant-rada (etter å fyrst ha oppdaga og løyst at hostnamen alt var oppteken av den gamle, arkiverte `Test-vercel`-testrada — fjerna derifrå). Full render stadfesta via eit Playwright-script: `/config.js` returnerer korrekt tenant-config, sida hydrerer EKTE produksjonsinnhald (ikkje berre `config.js` sine seed-verdiar), `#admin`-passordfelt og `/workspace/`-innloggingsskjema vises korrekt, ingen konsollfeil utover dei kjende/aksepterte anon-401-ane. Test-hostnamen er fjerna att etterpå.
     3. **`CNAME`-fila i repoet skal IKKJE fjernast enno** — einaste grunnen til at GitHub Pages kan ta imot domenet umiddelbart att ved ein rollback.
     
     **Konklusjon: alle stadfestingspunkta Arkitekten kravde er no gjort — ingen tekniske blokkeringar attståande for sjølve DNS-cutoveren.**

   **DNS-CUTOVEREN FAKTISK GJENNOMFØRT same dag (2026-07-16), v0.37.1.** Brukar endra sjølv apex-A-posten for `vibeverk.no` hos registraren til `76.76.21.21` (Vercel sitt tildelte mål for prosjektet "vibeverk"). Stadfesta live (curl `--resolve` for å omgå lokal DNS-cache-forseinking): `Server: Vercel`-header, korrekt dynamisk `/config.js`, ekte hydrert produksjonsinnhald. `CNAME`-fila i repoet er urørt (rask GitHub Pages-rollback framleis mogleg om naudsynt).

   Brukar sette same dag opp fire nye videresendingar hos registraren (URL-forwarding, ikkje DNS-postar): `console.vibeverk.no`→`vibeverk.no/console`, `staging.vibeverk.no`→`vibeverk-j1yg.vercel.app`, `sunnvask.vibeverk.no`→`sunnvask-demo.vercel.app`, `workspace.vibeverk.no`→`vibeverk.no/workspace` (**oppdatert av brukar same dag** til å peike direkte hit, i staden for via den gamle `/intranet`-omdirigeringssnubben som vart brukt fyrst). Alle fem stadfesta fungerande via curl. Oppklaring undervegs: `sunnvask-demo.vercel.app` er IKKJE eit eige Vercel-prosjekt, berre eit alias-domene på det same `vibeverk-j1yg`-prosjektet (stadfesta via `vercel project ls`) — kun to Vercel-prosjekt finst totalt (`vibeverk` og `vibeverk-j1yg`), begge kopla til auto-deploy frå same GitHub-repo.

   **Ny enkel utviklingsfase-passordsperre** lagt til same dag i `middleware.js` (brukar sitt ønske: "ingenting er offentleg enno, ikkje Vibeverk heller") — HTTP Basic Auth styrt av `SITE_LOCK_PASSWORD`-miljøvariabel (sett på begge Vercel-prosjekt, Production+Preview), gjeld samstundes alle fem domena/aliasa sidan dei deler same middleware-fil. IKKJE ei sikkerheitsgrense, berre ei hindring mot tilfeldige besøkjande — fail-open viss variabelen manglar. Kjem i TILLEGG til dei eksisterande admin-/Workspace-/Console-innloggingane (uendra bak sperra). Verifisert grundig live med curl på tvers av alle domene og etter ein oppfølgjande deploy.

   **Migreringa er stadfesta HEILT FULLFØRT, ingenting attståande.** To punkt er eksplisitt vurdert og bevisst VALT å ikkje gjere noko med no, stadfesta av brukar — **ingen av dei er eit tryggleiksavvik**: (1) `SITE_LOCK_PASSWORD` står framleis som placeholder-verdien frå oppsettet — bevisst, informert val ("ekstremt usannsynleg at nokon skal besøkje sida"), og sjølve mekanismen er uansett designa som ei hindring, ikkje ei sikkerheitsgrense — alle faktiske tilgangskontrollar (RLS, admin-/Workspace-passord, Console-OTP) er urørte bak sperra. (2) `CNAME`-fila i repoet er urørt (rask rollback) — inneheld berre den offentlege domenestrengen, ingen hemmelegheit, null risiko.

3. **Personvern — sterkare dokumentasjon og gjennomgang.** Løftar opp i prioritet det som tidlegare stod som eit seinare steg (standardkontrakt, databehandlaravtale/DPA, personvernerklæring). Skal ha ein eigen, grundig gjennomgang av **Privacy/Compliance Advisor**, ikkje berre ei rask utfylling av malar — gjeld både den eksisterande produksjonskunden og den nye eksempelkunden frå punkt 1. Fylt ut frå malane i `docs/compliance/` med stadfesta fakta, aldri oppdikta funksjonalitet.

   **Presisert av brukar 2026-07-17: Vibeverk er ikkje eit registrert juridisk selskap i dag** — alt er bygd/koda som privatperson, ikkje lansert, ingen ekte kundar eller ekte data er i bruk. Difor er det ikkje poeng i å faktisk SIGNERE DPA-ar/kundeavtaler no — det gjeld berre når/om ekte kundar skal onboardast. **Nedskalert til:** førebu DOKUMENTASJON og FORSLAG (DPA-mal, standardkontrakt-mal, personvernerklærings-utkast) no, som planleggingsmateriale — ikkje eit hastesteg, og ingen faktisk innsamling/signering av Vibeverk sine eigne underleverandør-avtalar (Supabase/Resend/Vercel) trengst før dette faktisk vert aktuelt.
4. **Læringsdokument til brukaren sjølv — FULLFØRT 2026-07-16.** Brukar leverte den varsla prompten (opphavleg skrive for Codex): ein omfattande "Delivery Readiness, Architecture, Documentation & Onboarding Pass". Brukar bad eksplisitt om at Arkitekt- og Historikar-agenten skulle vurdere innhaldet KRITISK før noko vart bygd, ikkje ta det inn direkte — begge agentane konkluderte uavhengig av kvarandre at prompten sin eigen føreslegne, flate `/docs/00-13`-nummerering ville skapt eit konkurrerande sannheitstre ved sida av den alt etablerte strukturen (`docs/README.md`, `docs/architecture/`, `docs/decisions/ADR-0001–0012`, `docs/compliance/`, `docs/security/`) — og at prompten sitt eige døme på ein ADR ville dikta opp ei ubekrefta avgjerdshistorie, i strid med `docs/decisions/README.md` sin eigen regel om at eit kodemønster åleine ikkje er bevis for ei avgjerd.

   **Stadfesta av brukar: dette oppfyller punktet, scopa ned til dei fire reelt nye stykka begge agentane samstemte identifiserte**, bygd inn i EKSISTERANDE mappestruktur (ikkje prompten sin eigen nummerering):
   - `docs/onboarding/new-team-member-onboarding.md` — sjølve læringsdokumentet, pluss ei ordliste over interne systembegrep (stadfesta reelt hol — copy-style-guide sin ordliste er noko heilt anna, unngå-ord i kundetekst)
   - `docs/onboarding/safe-changes-guide.md` — trygt-å-endre-matrise for ikkje-tekniske tilsette (stadfesta reelt hol)
   - `docs/security/incident-and-escalation-guide.md` — hendings-/eskaleringsguide (stadfesta reelt hol, om enn usikkert kor mykje det faktisk trengst — tidlegare hendingar er alle handterte direkte, same økt, utan ein formell guide)
   - `docs/architecture/customer-delivery-checklist.md` — produkt/innhald/kvalitet-sjekkliste for kundeleveranse, eksplisitt kryssreferert mot `docs/compliance/customer-go-live-checklist.md` (GDPR/juridisk) sidan Arkitekten vurderte dei som reelt ulikt scope, ikkje duplikat

   **Reelt, urelatert funn oppdaga undervegs, IKKJE fiksa denne runda** (utanfor scope for denne oppgåva): `docs/architecture/roles-and-tenants.md` og `docs/security/security-baseline.md` skildrar framleis Console sin GAMLE innloggingsmodell (SUPERADMIN_EMAILS-allowlist FØR OTP, ADR-0004) — dette har vore feil sidan Fase 8 (2026-07-08, ADR-0009), som bytte Console til å autentisere mot `vibeverk-control` med `operators.status='active'`-sjekk ETTER OTP. Bør rettast av Project Historian i ein eigen runde.
5. **Automatisert livetest-agent (smoke-test) for heile plattforma.** Stadfesta 2026-07-13. Ein gjenbrukbar, skriptbasert (Playwright) testpakke som faktisk driv ein ekte nettlesar gjennom kjerneflytene — innlogging (web-admin/Workspace/Console), CRM, booking, tilbod/kontaktskjema, chat-widget, KB, kunngjeringar, oppgåver, brukaradmin, og Console sin onboarding-sjekkliste — i staden for berre jsdom-logikktestane (`test.js`/`test-workspace.js`) eller éin-gongs manuell Playwright-verifisering (som Fase 9 sin kanarikunde fekk). Skal køyrast mot `vibeverk-staging`/ein dedikert testtenant, ALDRI direkte og ukritisk mot produksjon (skriv/slettar ekte testdata). Tenkt både manuelt utløyst og som ein tidsstyrt rutine (cron/`schedule`) som varslar ved feil — særleg verdifullt rett etter kvar ny kunde-onboarding (t.d. Sunnvask, punkt 1), som ei repeterbar "er alt likt originalen"-sjekk i staden for tillit til éin manuell gjennomgang. Bygg gradvis: start med dei flytene som nyleg var buggy (brukarsletting, backup/restore, dashboard-snarvegar), utvid derifrå. Eige arbeidsstykke — bør gå via Arkitekt- eller QA-agenten for testdekning-design før bygging.

   **Status 2026-07-16 — PÅBYRJA, v0.37.4.** QA-agenten gjorde testdekning-designen (som kravd over) FØR koding: tilrådde `vibeverk-staging` via lokal `config.js`-ombytting (ikkje ein Vercel-nåbar staging-vert eller ny tenant-rad — verken Workspace eller Web-admin løyser tenant via hostnamn lokalt). Ny, separat skill `.claude/skills/smoke-vibeverk/` (bevisst ikkje ein del av `run-vibeverk`, som er hardkoda mot produksjon). Éin flyt bygd og kodeverifisert: `dashboard-shortcuts` (regresjonsdekning for 0.32.x sin kappløps-bug). **Ikkje verifisert live enno** — krev ein ekte admin-testkonto på `vibeverk-staging` (ikkje stadfesta å finnast) og staging sin anon-nøkkel (ikkje henta denne runda, fanga av harnesset sin eigen legitimasjons-eksponeringsgrense — krev eksplisitt brukarhandling). Attståande i QA sin tilrådde rekkjefølgje: `user-deletion`, `backup-restore`, full innloggingsmatrise, Console sin onboarding-sjekkliste (sist).

   **Status 2026-07-17 — begge flytene stadfesta PASS live mot ekte `vibeverk-staging`.** `dashboard-shortcuts` og `user-deletion` er no begge grøne. Fyrste live-køyring avdekte og retta fleire reelle hol i sjølve testverktøyet: staging mangla éin migrering (pusha), `runStagingSql()` sin JSON-parsing var reelt sett aldri testa (CLI-en returnerer pretty-printa JSON over fleire linjer, retta), `dashboard-shortcuts` sin KB-halvdel feila fordi `config.js` sin standard har `kb: false` (konfig-ombyttinga tvingar no dette på mellombels), eit manglande opprydding-sikringsnett (eit urydda testmedlem frå ein tidlegare feila køyring vart funne og fiksa), og `user-deletion` traff Supabase sin eigen auth-e-post-rate-limit (prosjektinnstilling, ikkje ein kodefeil) — brukar heva grensa til 100, deretter PASS. Sjå `docs/project/CHANGELOG.md` 0.40.1/0.40.2 for full detalj. Attståande i QA sin tilrådde rekkjefølgje: `backup-restore`, full innloggingsmatrise, Console sin onboarding-sjekkliste (sist).

6. **Motta e-post (inbound) — flytta opp frå "Later" 2026-07-17 (brukar), del av å ferdigstille CRM-historia.** Design ferdig sidan 2026-07-01 (steg 6f i det opphavlege planarbeidet): Message-ID-tråding via Resend, automatisk oppretting av ny Kontakt-lead + CRM-kunde ved manglande treff når nokon svarar på ein e-post frå Vibeverk-systemet.

   **Status 2026-07-17 — FYRSTE IMPLEMENTASJON BYGD (v0.43.0), IKKJE DEPLOYA/PUSHA ENNO.** Brukar valde eksplisitt "Bygg no, inkl. lettvekts-tryggingsventilen" etter Security- og Privacy-gjennomgangane av designet under. Ein FJERDE, kort Arkitekt-konsultasjon rett før koding retta sjølve datamodellen: i staden for eit nytt `outbound_emails`/`lead_messages`-tabellpar, gjenbruker koden `crm_comms` sitt alt fungerande utgåande e-post-mønster direkte (sjå `docs/project/CHANGELOG.md` 0.43.0 for full fillliste og grunngjeving). Ein Security Auditor- og Privacy Advisor-gjennomgang av den FAKTISKE KODEN (ikkje berre designet) er sett i gang før PR/merge, per CLAUDE.md sin standardregel om at designgjennomgang ikkje erstattar kodegjennomgang. Krev eksplisitt brukargodkjenning for sjølve Supabase-deployen (ny migrasjon + ny Edge Function) og for `git push`, heilt separat frå "bygg no"-godkjenninga for koden.

   **Arkitekt-konsultasjon 2026-07-17 (les-berre) retta den opphavlege 2026-07-01-designen sin "kritiske byggbarheitspunkt"**: leads/CRM-kundar vart FAKTISK flytta ut av `store`-blob-mønsteret og inn i ekte, normaliserte Supabase-tabellar (`leads`, `crm_customers`, `crm_bedrifter`) 2026-07-03 — TO DAGAR etter at inbound-e-post-designen vart pausa, som ei separat retting av eit ikkje-relatert CRITICAL-funn (anon fekk ubetinga lesetilgang til heile `store`-tabellen). Det opphavlege "blob vs. normaliserte rader"-spørsmålet eksisterer difor ikkje lenger i den forma. Arkitekten fann i staden to ANDRE, ekte, framleis opne problem:
   - **Admin-cache vert aldri oppdatert medan panelet er ope** — `_leads`/`_customers` vert lasta éin gong ved oppstart/innlogging, aldri på nytt. Verken leads/CRM-kundar er lagt til i `supabase_realtime`-publikasjonen (i motsetnad til chat). Ein rad ein Edge Function skriv inn medan ein admin har fana open, vil difor IKKJE dukke opp før reload. Dette er ein føresetnad for at inbound e-post skal vere brukande live, ikkje noko unikt for e-post-arbeidet — bør rettast fyrst, via same pollingmønster som resten av kodebasen alt bruker (hent på nytt med jamne mellomrom, slå saman etter id, ALDRI erstatt heile arrayet, for å unngå å overskrive ein admin sin eigen, nyleg optimistisk oppdatering).
   - **Ekte kunde-e-post-dedup-race**: `findOrCreateCustomerByEmail()` sitt tenkte mønster (same som `autoImport()` sin `upsert()` i `module-crm.js` alt gjer) er check-then-insert utan databasenivå-eintydigheit på e-post — ufarleg i dag sidan det berre køyrer synkront frå éin admin sin nettlesarfane, men TO samstundes innkomande e-postar frå ein heilt ny avsendar (eller Resend som prøver ein webhook på nytt) kunne skape duplikate kundar. Løysing: eit nytt `SECURITY DEFINER`-RPC (same mønster som eksisterande `insert_anon_lead()`) som serialiserer på e-postadressa inni éin transaksjon (`pg_advisory_xact_lock`), IKKJE ein hard `UNIQUE`-skranke (kodebasen har alt ein admin-vend `merge_crm_customers()`-RPC for å slå saman utilsikta duplikatar — duplikate e-postar er ein kjend, tolerert, manuelt-reparerbar tilstand i dag, ikkje noko skjemaet forbyr).
   - Konkret elles: ny `inbound_emails`-tabell (idempotens mot webhook-gjentak + revisjonsspor for SPF/DKIM/DMARC-resultat, IKKJE ein blob-omgåing), nye `SECURITY DEFINER`-RPC-ar (skrivetilgang berre for `service_role`, same mønster som `insert_anon_lead()`, IKKJE ein rå `service_role`-tabellskriving), og `send-reply`-funksjonen bør lagre `resend_message_id` med det same (køyrer alt med den innloggte admin sin eigen økt, treng ikkje ein ny service-role-veg for akkurat den halvdelen).
   - **Viktig for neste steg**: den opphavlege 2026-07-01-designen sin skildring av leads/CRM som ein "blob delt med eit aktivt admin-UI" er UTDATERT og må rettast FØR Security Auditor/Privacy Advisor-gjennomgangen — elles analyserer dei ein risiko som ikkje lenger finst og kan gå glipp av dei to som faktisk finst (webhook-idempotens, kunde-e-post-racen).

   **Security Auditor-gjennomgang gjennomført 2026-07-17 (les-berre, av det korrigerte designet over) — ingen CRITICAL, 4 HIGH må inn i designet før koding:**
   - **H1**: Tråd-matching via `In-Reply-To`/`References`-headera er forfalskbar — kven som helst kan setje desse til ein tidlegare observert/gjetta verdi og injisere ei melding inn i EIN EKSISTERANDE kunde sin tråd. Matcha-tråd-innsetjingar må difor OGSÅ krevje DKIM/SPF-pass (domenegrunngjeving), ikkje berre stole på eit header-treff — same krav som for ny-oppretting, ikkje mindre strengt.
   - **H2**: Signaturverifisering må styre ALT som kjem etterpå (inkl. sjølve Resend-henteka llet) — verifiser FØRST, avvis umiddelbart ved feil, FØR noko som helst anna køyrer. Må handhevast som feil-lukka kodestruktur, ikkje berre skildrast som eit steg.
   - **H3**: Bruk det offisielle svix-biblioteket for signaturverifisering (handterer tidsstempel-/repriseverm), IKKJE ei eigenutvikla HMAC-samanlikning som lett kan gløyme denne kontrollen.
   - **H4**: Dei nye RPC-ane er `service_role`-only (webhooken kallar dei, aldri ein nettlesar) — treng eksplisitt `REVOKE ALL FROM PUBLIC, anon, authenticated` + `GRANT EXECUTE TO service_role`, og må vere `VOLATILE` (dei skriv data), ikkje `STABLE` som `insert_anon_lead()` sine lesefunksjonar.
   - MEDIUM verdt å merke seg: idempotens treng ein reell tilstandsmaskin (`received`→`matched`/`unmatched_created`/`rejected_*`), ikkje berre `ON CONFLICT DO NOTHING` (som ville la ein retry etter delvis feil stille hoppe over heile prosesseringssteget); sanering må vere STRENGARE enn det eksisterande `crm_comms`-mønsteret (autentisert-admin-forfatta innhald) sidan dette er ekstern, uautentisert avsendarinnhald — inkl. eit reelt sporingspiksel-personvernpoeng (eksterne `<img>`-kjelder i HTML-e-post kan avsløre for avsendaren når/om ein admin opna meldinga).

   **Privacy/Compliance Advisor-gjennomgang gjennomført 2026-07-17 (les-berre, av same design) — fann noko meir substansielt enn implementasjonsdetaljar:**
   - **Sentralt opent spørsmål**: automatisk profiloppretting av ein HEILT UKJEND, uverifisert avsendar (nokon som berre "hamnar" i ein catch-all-adresse — svar-alle, vidaresendt e-post, eller eit falskt negativt trådmatch) manglar den sjølvvalde relasjonen eit vanleg web-skjema-lead har. Truleg treng dette legitim interesse som rettsleg grunnlag (ikkje same grunngjeving som web-leads), inkludert ei eiga interesseavveging — **dette er ei EKTE juridisk vurdering, ikkje noko denne gjennomgangen eller kode åleine kan avgjere.**
   - **Github-verifisert, viktig kontekst**: `autoImport()` i `module-crm.js` er alt eit eksisterande presedens for automatisk profiloppretting UTAN menneskeleg gjennomgang (frå web-leads/bookingar) — skilnaden er kva som TRIGGA oppføringa (eit sjølvvalt skjema, kontra ein e-post som berre lander i innboksen).
   - **Forslag som IKKJE krev å reintrodusere ein manuell kø** (det eksplisitte brukarkravet frå 2026-07-01 står ved lag): automatisk oppretting som i dag føreslått, PLUSS eit "nyleg auto-oppretta, ikkje verifisert av eit menneske"-flagg/-vising i CRM-UI-et, PLUSS ein bulk-identifiser/bulk-slett-funksjon for desse (skilt frå `merge_crm_customers()`, som er for ekte duplikatar, ikkje spuriøse oppføringar).
   - **`inbound_emails` treng sitt EIGE formål/retensjon**, skilt frå CRM-data — ho loggar metadata om absolutt ALLE som sender til catch-all-adressa (inkl. spam/avvist post som aldri vart nokon kunde), nærare eit tryggingslogg-formål enn eit kundeoppfølgingsformål. Må avklarast eksplisitt om full e-posttekst eller berre metadata lagrast for avviste e-postar.
   - **Resend-personvernerklæringa må oppdaterast** før funksjonen er live — Resend prosesserer no innhald frå tredjepartar som ALDRI har hatt noko forhold til kunden, ein kvalitativt ny kategori registrerte for den leverandøren.
   - **Anbefaling**: ei eksplisitt DPIA-screening (ikkje nødvendigvis full DPIA) som eige steg før bygging — kombinasjonen "automatisk oppretting" + "uverifisert kjelde" + "potensielt særlege kategoriar i fritekst" (t.d. ein sjukmelding sendt til feil adresse) er nett den typen som typisk utløyser ei DPIA-vurdering.
   - **Eksplisitt IKKJE avgjort av gjennomgangen** (krev ekte juridisk rådgjeving før ein REELL, ikkje-demo crmFull-kunde går live med dette): (1) om legitim interesse er tilstrekkeleg rettsleg grunnlag for automatisk profiloppretting frå ein uverifisert avsendar, (2) om ei bulk-sikringsventil er rettsleg NAUDSYNT eller berre god praksis, (3) om SCC framleis er eit forsvarleg overføringsgrunnlag for Resend når dei no prosesserer tredjepartsinnhald, (4) om funksjonen treng ei formell DPIA.
   - Kryssref `project_missing_security_auditor_agent`-minnet og brukar sin eigen stadfesting 2026-07-17 om at Vibeverk ikkje er eit registrert juridisk selskap enno, ingen ekte kundar/data er i bruk — dei djupare juridiske spørsmåla over er difor IKKJE ei blokkering for å byggje/prototype vidare no, berre for at ein REELL crmFull-kunde nokon gong går live med funksjonen utan at dei er avklara.

   **Brukar sin presiserte visjon (2026-07-17), etter å ha spurt om noko "Tidio/HubSpot-liknande"**: IKKJE eit fullverdig CRM med SLA/eigenskapar/pipeline — eksplisitt for komplisert. Målet er ei "mikro/mini-CRM"-tidslinje: innkomande chat OG e-post med FAKTISK INNHALD synleg kronologisk på "Kunder"-sida, ikkje berre eit "hopp til chat"-snarvegsknapp slik det er i dag. Den polymorfe `crm_comms`-tabellen (alt brukt for e-post/telefonnotat/interne notat/dokument/oppgåve) er nøyaktig det rette verktøyet for dette — chat manglar berre som sin eigen `crm_comms`-oppføringstype. Denne biten (chat inn i tidslinja) krev INGEN ekstern input og difor inga Security/Privacy-gjennomgang, og kan byggjast uavhengig av og FØR inbound e-post — sjå økta 2026-07-17 for framlegg om rekkjefølgje.

## Later

**Stadfesta 2026-07-13 (brukar): fleire av punkta under er faktisk viktige og bør omprioriterast etter kvart.** Dei står likevel under "Later" (ikkje "Next") fordi ingen konkret prioriteringsrekkefølgje mellom dei er sett enno. Heile denne seksjonen (inkludert det som tidlegare stod under ei eiga "Ideas/Parking lot"-overskrift) har no fulle forklaringar, ikkje berre stikkord — slik at me hugsar KVA kvart punkt faktisk var/betydde, sjølv lenge etter at det vart notert.

**`customModules`-manifest (Fase 10) er fjerna som eige punkt 2026-07-17 (brukar):** slice 1+2 er ferdig bygde, merga og deploya, og heile pipelinen er stadfesta i praksis (Spaceship-testcaset, v0.41.2 — sjå `docs/project/CHANGELOG.md`). Det einaste attverande — det fyrste ekte `module-custom-<kunde>-<funksjon>.js`-eksempelet — er no berre vanleg, framtidig utviklingsarbeid når ein reell kunde faktisk ber om det, ikkje eit ope roadmap-punkt å spore.

**Chat Realtime (tidlegare "Steg 6d") er fjerna som eige punkt 2026-07-17, stadfesta ALT FERDIG BYGD, ikkje berre delvis:** admin-sida har ekte, verifiserte `postgres_changes`-abonnement på både `chat_conversations` og `chat_messages` (`module-chat.js` ~line 1811-1848) sidan tidleg i prosjektet. Vitjar-sida (den offentlege chat-widgeten) bruker MEDVITE berre polling via `get_visitor_msgs`-RPC-en, PERMANENT av design — ikkje ein mellombels løysing som skal "fullførast" — sidan Realtime for den sida ville kravd anon `SELECT`-tilgang på `chat_messages`, noko CLAUDE.md sin standardregel eksplisitt forbyr. Det er difor ingenting att å byggje her.

- **Steg 6f — Motta e-post (inbound).** Design ferdig (Message-ID-tråding via Resend, automatisk ny Kontakt-lead + CRM-kunde ved manglande treff når ein kunde svarar på ein e-post frå Vibeverk-systemet). Sett på vent av brukar 2026-07-01. **Brukar ønska 2026-07-17 å ta opp att dette** (del av å ferdigstille CRM-historia) — sjå eige punkt under "Next" for aktiv status. Må gjennom Security Auditor + Privacy Advisor før bygging (mottek og lagrar ekstern e-post-tekst, potensielt sensitivt).
- **Full i18n / engelsk språkstøtte.** Ingen `t()`/`STRINGS`-infrastruktur finst enno; i dag er all tekst hardkoda bokmål/nynorsk-blanda direkte i kvar modul-fil. Ei større arkitekturoppgåve (sentral strengkatalog, per-kunde/per-brukar språkval), ikkje ein enkel oversetjingspass. Relevant om Vibeverk nokon gong får ein ikkje-norsk kunde.
- **Support-tilgang for Console-operatørar inn i kunden sin eigen CRM/leads/bookings — RETTA 2026-07-17, var stale.** Denne oppføringa skildra tidlegare dette som "ikkje bygd" — FEIL, stadfesta ved kodesøk: `generate_support_access`-actionen i `supabase-control/supabase/functions/tenant-admin/index.ts` (bygd 2026-07-13, del av same runde som `invite_tenant_admin`/`configure_tenant_smtp`) dekker NØYAKTIG det tilrådde konseptet — mintar ei genuint tidsavgrensa Supabase magic-link for ein EKSISTERANDE ekte admin-brukar hos kunden (impersonering via ekte identitet, ingen fantom-/standing-konto), avviser arkiverte tenantar, og **loggar allereie** kven som spurte, for kven, og når til `broker_audit_log` (aldri sjølve lenka/token, berre hendinga). Dette er IKKJE full embed, og IKKJE ein Console-operatør med ståande tilgang — akkurat mekanismen som vart etterlyst.
  - **Privacy-gjennomgang FULLFØRT 2026-07-17.** Sjølve mekanismen vurdert som solid (ekte admin-identitet, ingen phantom-kontoar, tidsavgrensa, lenka aldri persistert, forespurnaden logga). Reelle, ikkje-blokkerande funn: (1) ingen kundevendt disclosure finst enno (personvernerklæring/DPA nemner ikkje at Vibeverk-tilsette KAN logge inn som kunden sin eigen admin) — bør leggjast til FØR ein reell kunde, ikkje no; (2) éin gong brukt, er operatøren sine handlingar UMOGLEG å skilje frå den ekte admin-brukaren sine eigne (ingen eige author_id/attribusjon) — ein reell, men akseptert avgrensing; (3) mindre, valfrie forbetringar identifisert (avgrens `broker_audit_log`-lesetilgang for desse radene til superadmin, ikkje alle aktive operatørar; ingen retensjonspolicy sett; eit alt-passert-men-ubrukt `?support=1`-flagg kunne merkje økta som support-relatert). **Brukar sin eksplisitte avgjerd 2026-07-17: berre logg funna, ingen kodeendring no** — funksjonen er medvite smal/nisje ("fint å ha når det trengs"), og brukar peika på at skjermdeling e.l. uansett er den venta, reelle støttekanalen i praksis, ikkje denne mekanismen åleine. Disclosure-teksten (punkt 1) står som eit ope, ikkje-urgent "før reell kunde"-punkt, resten er medvite ikkje prioritert.
- **Web-admin som rein separat SPA — FJERNA som eige punkt 2026-07-18 (brukar).** Stod tidlegare her som ei generell, arkitektonisk uavhengig oppryddingsoppgåve. Brukar sitt konkrete, underliggande behov for dette ("sidebygger treng meir plass enn dagens modal-admin") er no dekt av fullskjerm-modus (v0.43.3) på det EKSISTERANDE adminpanelet i staden — vurdert (to Arkitekt-konsultasjonar) som ein mykje mindre, lågare-risiko veg til same praktiske resultat enn ein full SPA-omskriving. Sjølve SPA-spørsmålet er difor ikkje lenger noko konkret å byggje; dukkar det opp eit NYTT, distinkt behov for det seinare, bør det loggast som sitt eige punkt då, ikkje gjenopplivast herifrå.
- **Steg 9 — Kvalitetssjekkar.** Full gjennomgang av alle modular, cross-device testing, tilgjengelegheit (WCAG), ytingstest (Lighthouse). Overlappar delvis med punkt 0 sitt forklaringstekst-arbeid (begge er brukarvendt kvalitet), men denne er breiare (også reint teknisk yting/tilgjengelegheit, ikkje berre tekst/forklaring).
- **`hub/tenants.js` — reell autentisering + flytte kundedata til Supabase.** Parkert 2026-07-01, framleis eksplisitt utanfor scope — Hub vert ikkje brukt for kundar no, og repoet er stadfesta offentleg på GitHub, så plaintext kundedata i denne fila ville vore lesbart i git-historikken uansett kva innlogging som står framfor `hub/index.html`. Må takast opp att FØR ein reell (ikkje-eigen) kunde vert lagt inn i Hub, OM Hub i det heile framleis er den tiltenkte vegen no som Console/tenant-registeret finst (usikkert — kan vere heilt avløyst av Console).
- **Steg 10 — AI-native Chat.** Ein AI-assistent bygd inn i den native chat-løysinga (RAG via pgvector, KB/FAQ/CRM/Booking-kontekst som kunnskapsgrunnlag, hybrid AI/operatør-modus der ein menneskeleg operatør kan overta). Eksplisitt spekulativ forretningsmodell-utforsking, ikkje eit forplikta steg — nemnt i tidlegare produktvisjon, ingen konkret plan.
- **Custom design-modul (Console-panelet) — retta 2026-07-16 (Project Historian), sjå avviksnotatet under. FERDIG, dette punktet dekker BERRE Console-sida.** Sjølve tema-editoren er ALT bygd og i aktiv drift: Console sitt "Web"/"Workspace"-tema-panel (`renderWeb()`/`renderWorkspace()` i `console/console-core.js`) har fargeveljarar (primær/sekundær/bakgrunn/tekst/overflate), eit kuratert 11-par skriftval med live førehandsvising, fri tekst-fontoverstyring og ein nullstill-til-standard-knapp — alt skrive gjennom `saveSC()` → `set_config`-brokaren → kunden sin eigen `applySuperConfig()`/`applyTheme()`-pipeline i `core.js`, presis slik `docs/arkitekt-notat-steg2.md` seksjon 4 tilrådde ("Kan leverast som eit Console-panel sidan det ikkje er direkte i kundeadmin"). Sjå `docs/project/CHANGELOG.md` for panelet si vedlikehaldshistorikk (m.a. skriftpar-talet utvida frå 5 til 11). **Dette panelet er MEDVITE ei mellombels/interim-løysing** — brukar (2026-07-17) stadfesta at Console-panelet dekker berre eit avgrensa, Vibeverk-operatør-styrt sett (farge/font/logo/radius), IKKJE den djupare, kunde-admin-styrte design-tilpassinga skildra i punktet rett under, som er noko heilt anna og enno ikkje bygd.

  **Dei tre attverande hola er no bygde (v0.38.1, 2026-07-16)**: logo-**fil**opplasting med allowlist-basert SVG-sanering (ny `upload_logo`-action i `broker`, sjå `docs/project/CHANGELOG.md`), eit hjørne-radius-val (`sc.colors.radius` → CSS-variabelen `--radius`), og ein WCAG AA-kontrastvalidator (reint klientside). Tryggingsgjennomgått før merge (fil-opplasting/lagring, per CLAUDE.md sin standardregel) — to MEDIUM-funn retta same runde. `broker`-funksjonen vart deploya til `vibeverk-control` same dag, boot-verifisert, og **stadfesta fungerande ende-til-ende av brukar 2026-07-17** (ekte, for stor logofil lasta opp via ein ekte operatørøkt).

  **Utvida vidare same dag (v0.38.2–v0.40.0, 2026-07-17), etter ein live-test-runde av brukar**: knapp-radius-feilen frå v0.38.1 (knappar var urørt av radius-valet) vart retta (`--btn-radius`, held pill-form på Standard/Runde). Logo-opplasting fekk automatisk PNG/JPEG-komprimering over 300KB (`imagescript`, tryggingsgjennomgått — eitt HIGH-funn om dekomprimeringsbombe retta med ein pre-decode dimensjonssjekk, deploya og boot-verifisert). Kontrastvalidatoren fekk ein «Generer forslag»-knapp (justerer éin feilande farge til AA-terskelen er nådd) OG ein separat «Generer fargepalett»-knapp (set saman eit heilt tema — primær/sekundær/bakgrunn/tekst/overflate — rundt ein tilfeldig fargetone, med same AA-garanti). Sjå `docs/project/CHANGELOG.md` for full detalj per delpunkt.

- **Design-modul ("sidebygger") — kunde-sjølvbeteningsdesign i Web-admin. Konsoliderer tre tidlegare, spreidde punkt (sjå historikk nedst) til éin plan, avklart og PÅBYRJA 2026-07-18.** Eit betalt tillegg (`features.sidebygger`, Console-only entitlement, same mønster som `crmFull`) der kunden VEL, i ein ny eigen "Design"-fane FYRST i Web-admin sin fanerekkjefølgje (Design | Innhold | Henvendelser | Innstillinger | Min konto), mellom fleire heile, namngjevne design-MALAR for heile nettsida — ikkje per-seksjon-variantar, ikkje eit fritt drag-and-drop-verktøy (eksplisitt avvist av brukar, "litt heftig"/for stor kompleksitets-/tryggingskostnad, jf. `docs/STRATEGY.md`). Byrjar med "Klassisk" (dagens design) og veks gradvis (2 → 5-10 malar over tid), medvite ikkje ferdigdesigna frå dag éin. To inntektsstraumar: betalt tilgang til det kuraterte galleriet, PLUSS betalt bestillingsarbeid for ein heilt ny, skreddarsydd mal til éin spesifikk kunde (eige `customTemplates`-felt per kunde sin `config.js`, aldri synleg for andre tenantar — same prinsipp som `customModules`).

  **Teknisk retning, stadfesta av to Arkitekt-konsultasjonar 2026-07-18**: `content.designTemplate` (nytt felt i den alt eksisterande `content`-blob-en, same Store/Supabase-synk som `nav-settings`). Kvar mal er sin eigen fil (`template-<namn>.js`) som registrerer seg i eit nytt, eige `window.SiteTemplates`-register (MEDVITE IKKJE via `App.registerModule()`) — held `components.js` sine `hero()`/`about()`/`services()` som tynne, uendra vidaresendarar uansett kor mange malar som finst seinare, i staden for eit veksande grenetre inni delte filer.

  **Fase 0 (infrastruktur + "Klassisk" portert inn uendra) BYGD OG MERGA (v0.44.0)** — ny fil `template-klassisk.js`, `features.sidebygger:false` i `config.js`, `ADMIN_CATEGORIES`/`allowedCategoriesForRole()`/`buildAdminTabs()`/`renderAdminTab()` utvida i `core.js`, ny `adminDesign()`-fane. Stadfesta både med testsuitene og direkte i nettlesar.

  **Console-toggle + grunnleggjande farge/font i Web-admin (v0.44.1)**: `sidebygger` lagt til i Console sin `FEAT_LABELS`/`FEAT_HELP` (av/på-styrbart per kunde via "Modular"-fana), og `adminDesign()` utvida med eit grunnleggjande farge-/font-panel (5 fargeveljarar, hjørne-radius, display-/brødtekst-font) som skriv til DEN SAME `superconfig`-nøkkelen Console sitt "Web"-panel bruker.

  **Synk-åtvaring (v0.44.2)**: brukar spurde om Web-admin og Console sine farge/font-redigeringar faktisk er synkroniserte — stadfesta dei deler nøkkel, men Console sin `saveSC()` sender heile operatøren sitt eige, potensielt eldre objekt (ikkje ein fersk refetch), så siste lagring vinn uansett kven som redigerte sist. Brukar vurderte kollisjonsrisikoen som lita i praksis og valde ei enkel åtvaring i staden for å byggje om lagre-flyten: ein oransje banner øvst i Console sitt "Web"-panel, synleg berre når den viste kunden har `features.sidebygger` aktivert.

  **Fase 1 — "Panorama", fyrste faktisk nye malen (v0.45.0)**: bilete-dominert motsetnad til Klassisk (fullbreidde 100vh-forsidetopp med botnforankra tekst, breitt 21:9-bilete i Om oss, biletkort-rutenett med botn-overlegg-tittel i Tenester, biletlaus korttilbakefall). Eigen fil `template-panorama.js`, isolert `.pano-*`-CSS, stabile anker-id-ar og delt `sanitizeRichHtml()` følgde. UX/Mobile Reviewer-gjennomgang gjennomført (påkravd for Fase 1, ny markup). Live nettlesar-stadfesta.

  **Farge/font på full djupne + sjølvbetent logo-opplasting (v0.46.0) — tre av fire punkt brukar bad om å logge 2026-07-18, no BYGD OG MERGA**: (1) Web-admin sitt farge/font-panel fekk WCAG-kontrastvalidator, "Generer forslag"/"Generer fargepalett" og nullstill-til-standard, PORTA ordrett frå Console sitt "Web"-panel; (2) same kuraterte 11-par skriftliste som Console PLUSS ei Google Fonts-rettleiing; (3) sjølvbetent logo-opplasting via ny `Media.putLogo()` — MEDVITE raster-berre (PNG/JPEG/WebP, ingen SVG), sidan trygg SVG-sanering krev ein server-funksjon Web-admin ikkje har tilgang til (i motsetnad til Console sin `upload_logo`-broker, som har akkurat ei slik allowlist via kontrollplanet) — og krev ei ekte Supabase-tilkopling (ingen lokal fallback), sidan `company.logoUrl` vert brukt direkte som `<img src>` i sidehovudet utan noko oppløysingssteg. Tryggingsgjennomgang + UX/Mobile Reviewer-pass gjennomført.

  **Design-fana delt i 5 underfaner + Tagline/SEO kundevendt + visuell opprydding (v0.47.0)**: brukar gav konkret tilbakemelding på det live panelet (skjermbilete av "Fargar") — meir luft rundt "Generer fargepalett", større/penare fargeboksar, "Hjørne-radius" bytt frå plain `<select>` til ein visuell kort-veljar som VISER avrundinga/knappåtferda for kvart val. Same runde: **Firmanavn held fram Console-only** (brukar sin eigen vurdering — kan endre kundeidentiteten), men **Tagline OG heile "SEO og deling"-seksjonen** (meta-beskrivelse, delingsbilde, favicon) vart kundevendt redigerbare, porta frå Console. Panelet vart samstundes DELT i 5 underfaner (Mal | Firma | SEO | Fargar | Fontar) etter brukar sitt forslag om at eitt langt skjema vart for tett — same underfane-mønster som "Innhold"-kategorien alt bruker. UX/Mobile Reviewer-pass gjennomført, tre MEDIUM-funn retta (fargeboks-kort utan maks-breidde, manglande biletførehandsvising for delingsbilde, utydeleg nullstill-omfang).

  **Mal 3 — "Scroll-story", scrollytelling-mal (v0.48.0)**: brukar ønska å "meistre ein mal" til — eit scrollytelling-uttrykk der sida les som ein sekvens av store, biletdominerte augeblikk som opnar seg progressivt idet kunden scrollar. MEDVITE ingen scroll-jacking/pinning (bygd på den eksisterande `.reveal`-mekanismen i staden). Sentrert (ikkje botnforankra) forsidetopp; Om oss/tenester har fullbreidde bilete-bakgrunn med sentrert tekst overlagt (hovudskiljet frå Panorama sin bilete-så-tekst-under-kvarandre); tenestekort vert kvar sitt eige "augeblikk" i sekvens, fungerer best med 3–6 kort. Eigen fil `template-scrollstory.js`, same isolasjonsmønster. **Eksplisitt isolasjonstest-runde gjennomført** (brukar sitt direkte krav, no som det finst tre malar) — sykla gjennom alle tre malane fleire gongar, stadfesta null CSS-/klasse-lekkasje. UX/Mobile Reviewer-pass gjennomført, tre HIGH-funn retta (Om oss/tenestekort mangla alt-tekst OG lazy-loading samanlikna med Klassisk/Panorama sine tilsvarande bilete — omskrive til delt `C.coverImg()`; mørkleggingslag styrka for tekstkontrast) + eitt MEDIUM-funn (iOS Safari sin `100vh`-adresselinje-feil på stabla fullskjerm-seksjonar, lagt til `100svh`-progressiv forbetring).

  **Attverande, ikkje starta**: eitt ope spørsmål frå 2026-07-18-runden står enno att — kva ANNA (utover mal/farge/font/logo/tagline/SEO) bør kunden sjølv kunne styre i Design-fana (brukar sin eigen ordlyd: "fleire ting dei burde få gjere?") — treng ei eiga avklaringsrunde, ikkje spekulert vidare her.

  **Historikk (tre tidlegare, no konsoliderte punkt)**: (1) "Custom design-modul (kundeadmin-sida, djupare tilpassing)" — brukar sitt fyrste ønske var å vente på ein Web-admin SPA-refaktorering, men denne vart vurdert som eit for stort, fleire-økter-arbeid for berre "sidebygger treng meir plass" — løyst i staden med fullskjerm-modus (v0.43.3) på det eksisterande adminpanelet, sjå eige punkt lenger opp om sjølve fullskjerm-arbeidet (bygd, feilslått sentrerings-forsøk reverta same dag, sjå CHANGELOG 0.43.2–0.43.5). (2) Ein tidlegare "avviksnotat" som klargjorde at Console sitt tema-panel ALLTID var meint som ei mellombels/interim-løysing, ikkje det endelege svaret. (3) "Utvida kunde-sjølvbetening for sideoppbygging" (logga 2026-07-16, ufullstendig spesifisert) — denne planen ER den konkrete oppfølginga av akkurat det punktet.
- **PWA-manifest / Service Worker for Workspace.** Ville gjort Workspace installerbar som ein app og gjeve grunnleggjande offline-støtte. Nemnd i same tidlegare arkitektnotat, låg prioritet, ingen konkret plan.
