# Current State — Vibeverk

Concise, factual summary of what is actually implemented right now. Not a wishlist — see [`docs/roadmap/ROADMAP.md`](../roadmap/ROADMAP.md) for planned work. For architectural detail behind any claim here, see [`docs/architecture/`](../architecture/README.md).

**2026-07-19 rewrite note**: this file had grown into a long, chronological, changelog-style pile of dated entries (back to 2026-07-01), duplicating `docs/project/CHANGELOG.md` and contradicting its own stated goal above. It has been rewritten below into a genuinely concise summary of what's true today. The entire previous body (all dated entries, the full security-findings-status log, etc.) is preserved verbatim, not deleted, in [`docs/archive/current-state-history-2026-07-19.md`](../archive/current-state-history-2026-07-19.md) — consult that file for the detailed reasoning/history behind any summary claim below.

## Last verified

**Repository/code verification: 2026-08-10 (v0.131.0).** Kundeanalyse code, migration and local tests were inspected and independently reviewed (Security Auditor, Privacy and Compliance Advisor, UX/Mobile Reviewer), real permanent-delete capability added, the control-plane migration was applied to `vibeverk-control` and directly verified live, a real production crash (`parse5@8` ESM/CJS incompatibility) was found and fixed, and operator-selectable AI model (Haiku/Sonnet, explicit allowlist) was added in this round.

**Last full live tenant/deployment verification: 2026-07-19.** The live project facts and matrix below are an explicitly dated snapshot from that round, not proof that all later repository migrations/functions are deployed. See `docs/roadmap/ROADMAP.md` "Current focus"/"Next" for current planning.

### Code and test state
- Current code version is **0.131.0** (`console/console-core.js`'s `VIBEVERK_VERSION`).
- `test.js`: **733 OK / 0 FEIL**. `test-workspace.js`: **296 OK / 0 FEIL**. `test-api.js`: **109 OK / 0 FEIL** (local 2026-08-10 runs, re-verified multiple times this round). `test-customer-analysis.js`: **18 OK / 0 FEIL**, `test-customer-analysis-console.js`: **4 OK / 0 FEIL**.
- 33 files in `supabase/migrations/` as of 2026-08-10 (file count only, re-verified this round; the specific "newest migration"/deploy-pending claim below predates this round's scope — Kundeanalyse's own migration lives in the separate `supabase-control/` history, not here — and needs its own live re-check, not assumed current): newest was previously `20260806170936_server_side_daily_analytics_hash.sql`. Its companion `sidetelling-event` Edge Function is also code-ready but not deployed.
- **Live browser + Codex testing round (2026-07-19), against Sunnvask-demo and production**: found and fixed a real raw-Storage-error-leak bug on the quote-attachment path (0.61.0); found and removed a permanently-noisy console warning that fired on every page for every real tenant regardless of actual config completeness (0.62.0); confirmed the anon booking-taken-slots RPC (`get_taken_booking_slots()`) works correctly end-to-end (real anon REST call, zero PII, RLS on the raw `bookings` table still blocks anon `SELECT`); confirmed zero `PWTEST-`/`SMOKETEST-` residue anywhere in production; found and cleaned up two leftover `PWTEST-` leads/CRM-customers on Sunnvask-demo from this session's own earlier testing. Full detail: `.claude/skills/smoke-vibeverk/TEST-MATRIX.md` and `docs/project/CHANGELOG.md` 0.61.0/0.62.0. Sunnvask-demo's `booking-assets` is empty (no configured public booking resource) — a content/demo-setup gap, not a code gap, so the visual booking calendar hasn't been walked through end-to-end yet.

### Three delivery surfaces
Public website (`/`), Workspace (`/workspace/`), Vibeverk Operator Console (`/console/`) — unchanged from `docs/architecture/system-overview.md`, no drift found this pass.

### Two real tenants, plus two non-tenant Supabase projects

| Project | Ref | Role |
|---|---|---|
| `vibeverk` (production) | `clzczbyklgdtdhgjphup` | Vibeverk's own real, live production tenant |
| Sunnvask-demo | `nzgibflxodcwuhtaprrs` | The demo customer, onboarded 2026-07-14 via the real Console onboarding flow |
| `vibeverk-staging` | `syqnyfeponexmkdvnsga` | Shared dev/test project — not a tenant either test case runs against directly |
| `vibeverk-control` | `jxoglthrnshabqmdmnui` | Control plane (tenant registry), not a data-plane project |

### Historical migration / Edge Function snapshot (directly queried 2026-07-19; not current repo parity)

| Project | Migrations (`supabase_migrations.schema_migrations`) | `manage-user` | `send-reply` | `inbound-email` | `anon-media-upload-token` |
|---|---|---|---|---|---|
| Production | 20/20 applied, matched repo at snapshot time | ACTIVE | ACTIVE | ACTIVE | ACTIVE |
| Staging | 20/20 applied, matched production at snapshot time | ACTIVE | absent | absent | ACTIVE |
| Sunnvask-demo | 20/20 applied — **closed 2026-07-19** (was 11/20, missing 9 migrations back to its 2026-07-14 onboarding date; confirmed by direct query, then closed with `db push` + 3 function deploys, re-verified afterward) | ACTIVE | ACTIVE | ACTIVE | ACTIVE |

These values were directly confirmed on 2026-07-19 only. They must not be read as current parity with the repository's 28 migrations; the 0.106.0 sidetelling migration/function are explicitly pending deployment and staging verification.

### A real testing-strategy gap
`vibeverk-staging` still lacks `send-reply`/`inbound-email`, while Sunnvask-demo has both and is the current non-production target for end-to-end email testing. Whether those functions should also be deployed to staging remains open — see `docs/roadmap/ROADMAP.md` "Next".

## Implemented and verified

- **Control-plane/data-plane split**: `vibeverk-control` (tenant registry, operators, audit log) is separate from every customer's own data-plane Supabase project. Per-tenant `service_role` keys via Supabase Vault only. See ADR-0008/0009/0010.
- **Console** authenticates via OTP against the control plane (not a customer project), gated on `operators.status='active'` checked after OTP. All config writes go through the `broker` Edge Function.
- **Hostname→tenant→Supabase-project resolution** (`resolve_tenant_by_hostname()` + `middleware.js` + `api/tenant-config.js`) is the live path for real traffic, including `vibeverk.no` itself since the 2026-07-16 DNS cutover. GitHub Pages remains only as a rollback path (`CNAME` file untouched).
- **Semi-automated tenant onboarding** via Console's "Kundar" section, hard-gated on real schema/routing verification before a tenant can go `active`.
- **Roles**: `admin`/`editor`/`member` only (DB CHECK constraint). See `docs/architecture/roles-and-tenants.md` for the full matrix.
- **Backup/restore** ("Sikkerhetskopi"): nine tables covered via admin-gated `SECURITY DEFINER` RPCs (`export_backup_tables()`/`restore_backup_tables()`), not just a UI-hidden button. `notes` and chat deliberately excluded (documented decision, not a gap).
- **Storage**: a public `media` bucket (images, general attachments) and a private `crm-documents` bucket (signed-URL access, admin/editor only) — see `docs/archive/current-state-history-2026-07-19.md` for the design rationale. A per-visitor daily upload quota (`anon-media-upload-token` Edge Function + `bump_and_check_anon_upload_quota()`) closed an anon-abuse gap in production 2026-07-19 (`supabase/migrations/20260719*`).
- **Inbound email**: live in production since v0.43.0/0.43.1 (2026-07-17) and deployed to Sunnvask-demo in the 2026-07-19 parity round — Message-ID/DKIM/SPF-verified thread matching, auto-creates a Kontakt lead + CRM customer on no match, reuses the existing `crm_comms` timeline pattern. Not deployed to staging (see the dated matrix above).
- **Mini-CRM timeline**: chat and email both appear as `crm_comms` entries on the customer timeline (not just a "jump to chat" shortcut).
- **Design templates ("sidebygger")**: three site-wide templates (Klassisk, Panorama, Scroll-story), plus customer-editable Tagline/SEO/colours/fonts/logo in a dedicated Web-admin "Design" tab. **`features.sidebygger` is `true` on BOTH real tenants** (directly queried against `store.superconfig` on production and Sunnvask-demo 2026-07-19 — an earlier draft of this file wrongly claimed it was off everywhere, caught and corrected the same day by the user spotting it live in a screenshot of their own admin panel; don't trust this kind of claim without a direct query again). See `docs/roadmap/ROADMAP.md` "Later" for what's still open here.
- **`customModules` manifest**: pipeline built and proven end-to-end (a real per-customer custom module shipped and loaded). No second real customer module exists yet — ordinary future dev work, not an open roadmap item.
- **Support access**: `generate_support_access` mints a time-boxed, audit-logged magic link for an existing real admin at a tenant (impersonation via real identity, no standing/phantom account) — privacy-reviewed, no code changes required by that review.
- **Testing**: `test.js`/`test-workspace.js` jsdom harnesses (counts above), run in CI on every push. A separate Playwright smoke-test suite (`.claude/skills/smoke-vibeverk/`) exists against `vibeverk-staging` for `dashboard-shortcuts`, `user-deletion`, `backup-restore`, `crm-documents`, `member-role-scope` and `editor-role-scope` (all PASS live, `backup-restore` re-verified 2026-08-05 — this document previously and incorrectly listed it as not yet covered, a documentation-staleness gap, not a real one; see `SKILL.md`'s own status table, which was correct). `member-role-scope`/`editor-role-scope` (new 2026-08-05) close the main gap in the "full login matrix" item: earlier flows invited/removed throwaway users via the real UI but never actually logged in as them, so role-gated UI visibility was never exercised — these use the Admin API's `generate_link` to get a real non-admin session and confirm admin-only UI is genuinely hidden. Only one module in the app is role-gated at all today, so member/editor produce identical results — real coverage for the one gate that exists. Console's onboarding checklist and Console's own OTP gate are still not covered. The `run-vibeverk` anonymous-flow driver (`.claude/skills/run-vibeverk/`, always against production) was also re-run in full 2026-08-05 (home/kontakt/tilbud/booking/chat, all PASS) and extended to also exercise the chat widget's offline lead-capture fallback (`#vw-off-*`), which it previously had no coverage for at all.

### Modules added since the 2026-07-19 verification pass (not yet covered by the facts above)

- **Karusell** (`module-carousel.js`, v0.66.0, 2026-07-20): image/video carousel, own `App.registerModule()` entry per carousel instance, admin-CRUD under "Innhald". Auto (timed, `prefers-reduced-motion`-aware) or manual (arrows/dots/swipe) advance. Video scope deliberately limited to muted/looped `video/mp4`, 20MB max, own `media-video` Storage bucket. **Corrected in this pass (2026-08-03): there is no separate `carousel` feature flag** — the module gates on `CFG.features.sidebygger === true` (verified directly in `module-carousel.js`), the same paid "Design" flag as the templates/Banner section above. Since this file's own "Design templates" bullet confirms `features.sidebygger` is `true` on both real tenants, Karusell is **not** off-by-default in practice wherever `sidebygger` is already on — it is gated by that flag's state, not a dormant flag of its own. **Not yet production-ready**: the `media-video` bucket migration has not been run against any real Supabase project (needs its own approval, separate from the code merge), and a real-browser upload test with an actual mp4 hasn't been done yet.
- **Nettsidehelse** (v0.76.0–0.77.2, 2026-07-27): rule-based health check of the customer's public site — 4 categories (Synlegheit/Innhald/Tillit/Tilgjenge), 0–100 score + per-category score + traffic-light + top-5 prioritized fixes. Pure function (`computeWebsiteHealth()`), no external API/browser-rendering dependency (Core Web Vitals, sitemap/URL-structure, and keyboard-nav checks were deliberately cut from the original proposal as not meaningful for Vibeverk's single-hash-route architecture). Own tab in both Web-admin (Innstillingar → Nettsidehelse) and Console. See `docs/architecture/website-health-scoring.md`.
- **Console "Priser"** (`console/console-core.js` `renderPriser()` + ~20 helpers, v0.88.0, 2026-08-04): internal pricing/package-planning tool for Vibeverk's own staff — not real billing, not visible to any customer. New global (non-tenant-scoped) `pricing_config` table in the control plane (`supabase-control/supabase/migrations/20260804120000_add_pricing_config.sql`), SELECT-only RLS for `authenticated`, all writes via a new audited `set_pricing_config` action in `supabase-control/supabase/functions/tenant-admin/index.ts` (server-side validation incl. a price cap and a duplicate-package-id check) rather than direct RLS-CRUD — the same corrected pattern `tenants`/`operators` already moved to (direct authenticated writes bypass `broker_audit_log`). **Deployed and verified live against `vibeverk-control`, same day**: the PR merged to `main`, `tenant-admin` (with `set_pricing_config`) deployed and confirmed via CLI, and the migration itself was run manually through the Supabase Dashboard SQL Editor (no pooler connection string available that session) — then, per `CLAUDE.md`'s "a Dashboard 'Success' message confirms nothing" rule, checked with four explicit verification queries rather than trusted on a green result: row/seed data present, `relrowsecurity=true`, exactly one SELECT policy for `authenticated`, and a grants check that caught a real deviation — Supabase's platform-default ACL had also given `service_role` full CRUD (not just the SELECT/UPDATE the migration explicitly granted) — fixed with an explicit `REVOKE`+`GRANT` to match the documented least-privilege design, then re-verified. One known, non-blocking follow-up: because the migration ran via Dashboard rather than `supabase db push`, `supabase_migrations.schema_migrations` doesn't know it's applied — a future CLI `db push` against `vibeverk-control` could try to re-run it and fail on "table already exists" until someone runs `supabase migration repair 20260804120000 --status applied` (needs a pooler connection string, not done yet). Same-day Security Auditor (two LOW findings, fixed) and UX/Mobile Reviewer (two BLOCKER, several HIGH, fixed) passes. Full detail: `docs/project/CHANGELOG.md` 0.88.0.
  - **0.89.0 refinement round (2026-08-04, same day)**: collapsible Console sidebar (ported from Workspace's `.i-sidebar.is-collapsed` pattern, `localStorage`-persisted, correctly forced back to full width inside Console's own mobile hamburger breakpoint), a wide-mode CSS class (`.cs-content--wide`, 1400px) applied only to the Priser section, a "Last ned som bilde" PNG export of the customer-facing comparison view via a newly-pinned `html2canvas@1.4.1` CDN dependency (client-side only, no server round-trip), and three new per-package data-cap fields (`storageGB`, `emailsPerMonth`, `usersIncluded`, with `-1` as an "unlimited" sentinel) — informative numbers only, not technically enforced anywhere yet. `PKG_ALLOWED_KEYS` and a new `validateCapField()` validator were added to `set_pricing_config` in `supabase-control/supabase/functions/tenant-admin/index.ts` for the three new fields, but **that Edge Function change has not been redeployed yet** — pending explicit approval per the deployment safeguard, same as the 0.88.0 deploy above. No new migration needed (the cap fields live inside the existing flexible `pricing_config.data jsonb` column). Same-day independent Security Auditor (no BLOCKER/HIGH/MEDIUM) and UX/Mobile Reviewer (one HIGH — the sidebar-collapse preference leaking into the mobile hamburger menu — plus several MEDIUM, all fixed) passes. Full detail: `docs/project/CHANGELOG.md` 0.89.0.
- **Intern sidetelling / "Innsikt"** (`module-sidetelling.js`, v0.78.0, 2026-07-31; grouping rebuilt v0.106.0, 2026-08-06): cookie-free pageview/CTA counting as a free alternative to Plausible, public site only. `features.sidetelling` is off by default and yields to Plausible. **No analytics grouping ID is now read from or written to cookie/browser cache/`localStorage`/`sessionStorage`.** Each new event goes to `sidetelling-event`, which computes SHA-256 of UTC day + site Origin + request IP + User-Agent and sends only the daily pseudonym/event to a service-only write RPC. No salt table, key rotation, HLL or persistent token; a separate abuse-control row stores only date + global count. Historical client-ID rows remain readable. Daily grouping is approximate, not a unique-visitors KPI. Raw IP/UA are not stored in `analytics_events`, though Supabase may have infrastructure logs. Edge deploy + migration + staging header/quota/ACL verification remain pending explicit approval; real tenants stay paused, and legal basis/retention/privacy publishing remain launch gates. Conversion linking remains off. Full detail: `docs/architecture/sidetelling.md`, ADR-0013.
- **AI Lab** (`scripts/ai-lab-server.js` + `scripts/ai-lab/*`, v0.125.0, 2026-08-10, merged in current `main` history): internal, localhost-only Console developer tool for drafting and reviewing structured Læringsmodul proposals. It remains strictly separate from Læring, has no database and has no automatic publishing path. See `docs/architecture/ai-lab.md` for the security and provider boundaries.
- **Kundeanalyse** (`api/customer-analysis.js` + `api/_lib/customer-analysis-*`, v0.131.0, 2026-08-10): internal Console MVP for bounded analysis of public prospect sites. DNS-pinned/robots-respecting crawler, deterministic findings, optional structured Anthropic review, human approval, meeting brief and a real permanent-delete action (cascades to all pages/findings/briefs, leaves only a content-free audit trace) are implemented, reviewed by an independent Security Auditor/Privacy Advisor/UX Reviewer pass. Control-plane migration applied and directly verified against `vibeverk-control` (all 8 tables/RLS/policies/grants/function confirmed live) — this same push also caught and fixed a real pre-existing gap: `tenants.enabled_modules`'s new-tenant default had never actually taken effect despite an earlier migration's comment claiming it was applied via Dashboard. `VIBEVERK_CONTROL_SERVICE_ROLE_KEY` is now set in Vercel Production. First real production request crashed (`parse5@8` ESM/CJS incompatibility with Vercel's build step); fixed by downgrading to `parse5@7.3.0`, verified against a real Preview deployment via `vercel logs` before landing on `main`. Operators can now choose the AI model (Haiku, the default, or Sonnet) per run and per meeting-brief generation, validated server-side against an explicit allowlist (`resolveRequestedModel()`). No retention/deletion *schedule* exists yet (deletion is manual, per-analysis) — still an open launch gate, along with legal-basis/DPA sign-off. See `docs/architecture/customer-analysis.md`.

## Not implemented

- **Full i18n / English locale support** — no `t()`/`STRINGS` infrastructure. Last directly re-confirmed 2026-07-02; not re-checked this pass, but no evidence anything has changed.
- **PWA manifest / Service Worker** for Workspace — discussed only.
- **AI-native chat** (RAG, Claude API integration) — explicit future idea, no code exists.

## External verification required

- Sunnvask-demo's actual applied-migration list (see above) — needs a DB pooler connection string or a live `db push` run.
- Live DNS/HTTP state of `vibeverk.no` and its registrar-level URL-forwardings — repo-side evidence (`middleware.js`, ADR-0007's addendum) is confirmed; the live network state itself was not independently re-curled this pass.
- `SITE_LOCK_PASSWORD`'s actual current value on Vercel — project environment-variable configuration, not visible in this repository.
- Any Supabase Dashboard-only configuration (Auth settings, SMTP, storage bucket flags) for any of the three data-plane projects, beyond what a `functions list`/migrations query can confirm.

## Known limitations

Full detail (dead-code history, the `is-err`/`is-error` CSS-class drift precedent, the `.help-icon__pop` viewport-clamping gap, the `vibeverk-security-auditor`/`vibeverk-reviewer` Codex-only-agent gap, and others) lives in `docs/archive/current-state-history-2026-07-19.md`. Still true today, not superseded:
- **`vibeverk-security-auditor` and `vibeverk-reviewer` exist only as Codex-only configs** (`.codex/agents/*.toml`), not as Claude-invokable agents — `CLAUDE.md`'s "AI agent workflow" section names them as if directly invocable via the Agent tool, symmetrical with the five real `.claude/agents/*.md` entries. The working workaround is a `general-purpose` agent given the equivalent brief. Needs a user decision (author real `.claude/agents/` files, or reword `CLAUDE.md`), not resolved this pass.
- `AGENTS.md` and `CLAUDE.md` are maintained as two separate, near-duplicate files rather than a single generated source.
- `storageKey: "nordpunkt"` cannot be renamed without a full atomic data migration.
